import numpy as np, , # 実験データ, h = 6  # 落下高さ (m), t_exp = 2.3  # 実際の落下時間 (s), mass = 0.079  # 質量 (kg), rho = 1.206  # 空気密度 (kg/m^3), g = 9.8  # 重力加速度 (m/s^2), , # パラシュートの有効面積（円 - 中心の穴）, r_outer = 0.18  # 外側半径 (m), r_inner = 0.05  # 中心の穴の半径 (m), A = np.pi * (r_outer**2 - r_inner**2)  # 有効面積 (m^2), , # 自由落下における終端速度の推定（単純に平均速度を仮定）, v_avg = h / t_exp  # 平均速度, v_final = 2 * v_avg  # 単純な等加速度運動の仮定より終端速度を推定, , # 空気抵抗 Fd = 0.5 * Cd * rho * v^2 * A, # 平衡状態（重力と空気抵抗がつり合う）: mg = Fd, # よって Cd = (2 * mg) / (rho * v^2 * A), , Fd = mass * g, Cd = (2 * Fd) / (rho * v_final**2 * A), Cd